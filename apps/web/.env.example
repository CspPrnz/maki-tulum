# Build-time only (inlined into the bundle). Set as Railway "build variables".
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional. Absent = Plausible script not rendered / Sentry not initialized.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=
NEXT_PUBLIC_SENTRY_DSN=
